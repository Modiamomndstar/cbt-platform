services:
  # --- Database ---
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal

  # --- Backend API ---
  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/modiamomndstar/cbt-platform-backend:8d07ffb971afa8d1bd75a8d71877ad368f8b8e4b}
    restart: always
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      NODE_ENV: production
      FRONTEND_URL: ${FRONTEND_URL:-https://mycbtplatform.cc}
    env_file:
      - .env.production
    depends_on:
      - db
    volumes:
      - uploads_data:/usr/src/app/uploads
      - /home/opc/oci_api_key.pem:/usr/src/app/oci_api_key.pem:ro
    networks:
      - internal
      - web

  # --- Frontend ---
  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/modiamomndstar/cbt-platform-frontend:8d07ffb971afa8d1bd75a8d71877ad368f8b8e4b}
    restart: always
    networks:
      - web

  # --- Reverse Proxy ---
  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - web
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
  uploads_data:

networks:
  internal:
  web:
