:80 {
    # Reverse Proxy for Backend API
    handle /api/* {
        reverse_proxy backend:5000
    }

    # Serve uploaded files (school logos, etc.) from backend
    handle /uploads/* {
        reverse_proxy backend:5000
    }

    # Serve Frontend (SPA)
    handle /* {
        reverse_proxy frontend:80
    }
}
