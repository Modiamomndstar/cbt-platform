# =============================================
# CBT Platform - Production Environment File
# Copy this to the server at ~/cbt-platform/.env.production
# Fill in the real values - NEVER commit this to git
# =============================================

# --- Database ---
DB_USER=cbt_user
DB_PASSWORD=CHANGE_ME_strong_password_here
DB_NAME=cbt_platform

# --- Security ---
JWT_SECRET=CHANGE_ME_at_least_64_chars_random_string_here

# --- Application ---
NODE_ENV=production
FRONTEND_URL=http://145.241.97.246

# --- Email (Optional - for password reset emails) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your_app_password
